type Idea {
    id: ID!
    idea: String!
    description: String!
    created: String!
    update: String
    author: User
    upvotes: Int
    downvotes: Int
    comments: [Comment!]
}

type Query {
    ideas(page: Int, take: Int, newest: Boolean): [Idea!]
} 